import React from "react";

const IconButton = ({
     children
}: {
     children: React.ReactNode,
}) => {
     return (
          <div>
               {children}
          </div>
     );
}

export default IconButton;