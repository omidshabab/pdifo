import { PlusIcon } from "lucide-react";
import IconButton from "../IconButton";

const AddPageButton = () => {
     return (
          <IconButton>
               <PlusIcon size={18} />
          </IconButton>
     );
}

export default AddPageButton;